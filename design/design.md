┌ 顶栏(压缩高度): App 标题 · 右侧菜单(Gear/Help) ───────────────┐
├─────────── 左栏(可折叠, 默认 ~360px) ──────────┬───────────────┤
│ ┌ 来源:  🎙️ record │ 🎞️ file │ 🔗 link ──┐    │ ┌ 工具条 ──────┐│
│ └──────────────────────────────────────────┘    │ │ copy/保存/   ││
│ ┌ 输入参数(随来源 Tab 切换) ─────────────┐       │ │ 格式/方向 RTL ││
│ │ record: 输入/输出设备、录音名、存文档    │       │ └──────────────┘│
│ │ file  : 语言、拖放区、文件夹批处理开关    │       │ ┌ 片段/翻译/摘要┐│
│ │ link  : URL、保存位置                    │       │ │ Tab + 重新总结││
│ └──────────────────────────────────────────┘       │ └──────────────┘│
│ ┌ 音频控制(AudioPlayer/进度) ──────────────┐       │ ┌ 转录文本区 ──┐│
│ └──────────────────────────────────────────┘       │ │ (常驻, 撑满) ││
│ ┌ 主操作按钮 + 进度条 ─────────────────────┐       │ │  空态/预览   ││
│ │  [开始] / [停止并转写] / 取消            │       │ └──────────────┘│
│ └──────────────────────────────────────────┘       │                │
└──────────────────────────────────────────┬──────────┘                │
          ┆ 可拖拽分隔条 (可选)              ┆                          │
